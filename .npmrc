# Only install package versions that have been public for at least 7 days.
# Supply-chain guard: most malicious publishes are caught and unpublished
# within hours, so a cooling-off window filters them out before they land here.
# Unit is days. Mutually exclusive with `before`.
min-release-age=7
